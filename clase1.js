var nombre = 'Manuel Dario', apellido = 'Gallardo'

//convertir a mayuscula

var nombreEnMayus = nombre.toUpperCase()
//convertir a minuscula
var apellidoEnMinus = apellido.toLowerCase()

var primeraLetra =nombre.charAt(0)

var cantletras = nombre.length

var fullName = `${nombre} ${apellido.toUpperCase()}`
//subcadena
var str = nombre.substr(1,4)